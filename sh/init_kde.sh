#!/bin/bash

sudo apt install latte-dock
timedatectl set-local-rtc 1 # fix windows dualboot time issues


# TODO Manually because I haven't quite figured out the dotfiles way
# ctrl + space -> KRunner
# global menu (add widget)
